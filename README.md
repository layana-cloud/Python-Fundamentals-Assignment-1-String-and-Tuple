# Python-Fundamentals-Assignment-1-String-and-Tuple
https://colab.research.google.com/drive/1Mul18LDRySnQ4IZq04-E9-D4XMD_rfQO?usp=sharing

To understand and implement basic operations on Strings and Tuples in Python including concatenation, indexing, slicing, and built-in methods.
**1. String Concatenation**
- Created two strings `t1 = "Hello "` and `t2` as user input.
- Used `+` operator to concatenate and display the final greeting message.

**2. String Slicing and Indexing**
- Worked with string `t5 = "Hello Zara, welcome to Python programming"`
- Performed operations like accessing first/last character, slicing first 5 and last 5 characters, reversing the string, and extracting a specific word.

**3. String Methods**
- Used string `strM = "Python beginner tutorial"`
- Implemented methods like `upper()`, `lower()`, `capitalize()`, `count()`, and `replace()` to manipulate the string.

**4. Tuples**
- Created two tuples `t1=(10,20,30)` and `t2=(40,50,60)`
- Performed concatenation `t_combine = t1+t2`, repetition `*3`, accessing elements via indexing `t_combine[2]`, and slicing `t_combine[0:3]` and `t_combine[-3:]`.
